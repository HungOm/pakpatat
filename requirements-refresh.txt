# Extra packages for refreshing the archive from the live site.
#
# ONLY the operator who maintains the archive needs these. A case worker's
# install does not: the app answers from data/ and never fetches anything, and
# `refresh.py detect` (the nightly change check) is standard library only.
# Keeping them out of requirements.txt keeps the case-worker install small and
# keeps an HTML parser out of a machine that has no reason to parse HTML.
#
#     pip install -r requirements-refresh.txt

beautifulsoup4>=4.12      # parse the WordPress REST payload's rendered HTML
markdownify>=0.13         # HTML -> markdown, matching the original capture
