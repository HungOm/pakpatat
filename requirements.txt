# Core graph + provider abstraction
langgraph>=1.2
langchain>=1.3
langchain-core>=1.5

# Model providers. All are installed so the Settings panel can switch between
# them without a reinstall; the app uses whichever ASSISTANT_MODEL_PROVIDER
# points at.
langchain-ollama>=1.0            # DEFAULT: local, offline, no API key
langchain-google-genai>=4.2      # Gemini Flash
langchain-anthropic>=1.4         # Claude
langchain-openai>=1.3            # OpenAI

# Local retrieval -- no API key, works offline, no PyTorch (ONNX only)
fastembed>=0.8
rank_bm25>=0.2
numpy>=1.26

# Desktop window (uses the OS's built-in browser engine, not Electron)
pywebview>=6.2

# MCP server for Claude Desktop / Claude Code
mcp>=1.28

python-dotenv>=1.0

# Rendering an answer as a shareable PNG (pakpatat/postcard.py). Must be built
# with libraqm, or Burmese renders as disconnected glyphs -- postcard.py checks
# and refuses rather than producing text that looks right and is not.
Pillow>=10.0
